@using TAS.Resources
@{
    
}
@section Style {
    <link rel="stylesheet" href="../Contents/css/Farm/Farm.css">
}

<div class="RubberIntake-page">
    <div class="row">
        <div class="col-xl-12">
            <div class="card table-card">
                <div class="card-body">
                    <!-- ========================================
                         TOOLBAR
                    ======================================== -->
                    <div class="datatable-top " style="justify-content:flex-end">
                        <!-- Left side: Filters -->
                        <div class="button_filter_search">
                            <label>@Language.key_congcuthaotac</label>
                            <div>
                                <button type="button" id="btnAdd" class="btn btn-custom-tas-action btn-sm">
                                    <i class="fas fa-plus"></i> <span>@Language.key_themmoi</span>
                                </button>
                                <button type="button" id="btnRefresh" class="btn btn-custom-tas-action btn-sm">
                                    <i class="fas fa-sync-alt"></i><span>@Language.key_reload</span>
                                </button>
                            </div>
                        </div>
                        <!-- Right side: Actions -->
                        <div class="datatable-actions">
                        </div>
                    </div>

                    <!-- ========================================
                         AG-GRID TABLE
                    ======================================== -->
                    <div id="farmGrid" class="ag-theme-alpine" style="height: 600px; width: 100%;"></div>
                    <div id="divPagingContainer" class="ag-paging-panel ag-paging-button-wrapper"></div>
                    <!-- ========================================
                         HIDDEN FILE INPUT
                    ======================================== -->
                    <input type="file" id="fileImport" accept=".xlsx,.xls" style="display:none;" onchange="handleFileImport(event)" />
                </div>
            </div>
        </div>
    </div>
</div>


@* <partial name="~/Views/Farm/_AddEditModal.cshtml" />
<partial name="~/Views/Farm/_DeleteModal.cshtml" />
<partial name="~/Views/Farm/_MapModal.cshtml" />
<partial name="~/Views/Farm/_StatisticsModal.cshtml" /> *@

@section Scripts {
    <script type="text/javascript" src="~/Contents/js/geoMap.js"></script>
    <script type="text/javascript" src="~/Contents/js/Farm/Farm.js"></script>
    <script>
        var IsOptionAll = false;
        initFarmPage();
    </script>
}