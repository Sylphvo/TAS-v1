@using TAS.Resources
@{

}
@section Style {
    <link rel="stylesheet" href="../Contents/css/Agent/Agent.css">
}
@* <div class="row">
    <div class="col-12">
        <div class="card table-card">
            <div class="card-header">
                <div class="d-flex align-items-center justify-content-between">
                    <div class="d-flex align-items-center">
                    </div>
                    <div class="d-flex">
                        <!-- Nút chọn file -->
                        <label for="importExcelAgent" class="btn btn-custom-tas"><i class="ti ti-upload f-20"></i>&nbsp; <span data-i18n="key_importexcel"></span></label>
                        <input type="file" id="importExcelAgent" hidden>
                        <span id="file-name" class="ms-2 d-none"></span>
                        <button onclick="onExportTemplateExcel()" class="btn btn-custom-tas buttonSearch"><i class="ti ti-file-x f-20"></i>&nbsp; Mẫu Excel</button>
                        <button class="btn btn-custom-tas buttonSearch" data-bs-toggle="modal" data-bs-target="#AddNewRubberAgent"><i class="ti ti-plus f-20"></i>&nbsp;  <span data-i18n="key_themmoi"></span></button>
                    </div>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <!-- Action Buttons -->
                    <div class="row mb-3">
                        <div class="col-12">
                            <button type="button" id="btnAdd" class="btn btn-custom-tas">
                                <i class="fas fa-plus"></i> Thêm mới
                            </button>
                            <button type="button" id="btnBulkDelete" class="btn btn-custom-tas" disabled>
                                <i class="fas fa-trash"></i> Xóa đã chọn (<span id="selectedCount">0</span>)
                            </button>
                            <button type="button" id="btnExport" class="btn btn-custom-tas">
                                <i class="fas fa-file-excel"></i> Xuất Excel
                            </button>
                            <button type="button" id="btnRefresh" class="btn btn-custom-tas">
                                <i class="fas fa-sync"></i> Làm mới
                            </button>
                        </div>
                    </div>
                    <!-- [ Main Content ] end -->
                    <div id="agentGrid" style="height: 500px;width:100%" class="ag-theme-alpine"></div>
                    <div id="ag-paging-footer" class="ag-paging-button-wrapper"></div>
                </div>
            </div>
        </div>
    </div> 
</div>
<partial name="~/Views/Agent/_AddEditModal.cshtml" />
<partial name="~/Views/Agent/_DeleteModal.cshtml" />
<partial name="~/Views/Agent/_MapModal.cshtml" /> *@
<div class="RubberIntake-page">
    <div class="row">
        <div class="col-xl-12">
            <div class="card table-card">
                <div class="card-body">
                    <!-- ========================================
                         TOOLBAR
                    ======================================== -->
                    <div class="datatable-top " style="justify-content:flex-end">
                        <!-- Left side: Filters -->
                        <div class="button_filter_search">
                            <label>@Language.key_congcuthaotac</label>
                            <div>
                                <button type="button" id="btnAdd" class="btn btn-custom-tas-action btn-sm">
                                    <i class="fas fa-plus"></i> <span>@Language.key_themmoi</span>
                                </button>
                                <button type="button" id="btnRefresh" class="btn btn-custom-tas-action btn-sm">
                                    <i class="fas fa-sync-alt"></i><span>@Language.key_reload</span>
                                </button>
                            </div>
                        </div>
                        <!-- Right side: Actions -->
                        <div class="datatable-actions">
                        </div>
                    </div>

                    <!-- ========================================
                         AG-GRID TABLE
                    ======================================== -->
                    <div id="agentGrid" class="ag-theme-alpine" style="height: 600px; width: 100%;"></div>
                    <div id="divPagingContainer" class="ag-paging-panel ag-paging-button-wrapper"></div>
                    <!-- ========================================
                         HIDDEN FILE INPUT
                    ======================================== -->
                    <input type="file" id="fileImport" accept=".xlsx,.xls" style="display:none;" onchange="handleFileImport(event)" />
                </div>
            </div>
        </div>
    </div>
</div>



@section Scripts {
    <script type="text/javascript" src="~/Contents/js/Agent/Agent.js"></script>
    <script>
        var IsOptionAll = false;
        initPageAgent();
    </script>
}